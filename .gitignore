/target
/data/parquet/*.parquet
!data/parquet/block_00000.parquet
!data/parquet/block_00001.parquet
!data/ledger.jsonl
/data/parsers/*.json
/data/parsers/*.yaml
*.log
!sample_new_firewall.log
!data/raw/*
# P7.6: `*.log` matches at any depth — explicit negations for the new
# corpus subdirs (sidecars `.jsonl` are unaffected by any rule).
!data/raw/adversarial/*.log
!data/raw/holdout/*.log
audit*_dump.jsonl
# Full end-to-end dataset (generated on demand via --full; never committed)
data/raw/full/
/data/full_out/
# run_demo.sh scratch output (never the tracked data/parquet fixtures)
/data/demo/

# Python bytecode caches (py_compile checks)
__pycache__/

# `ulpf scorecard` demo output (one command regenerates it; eval_*.md stay committed)
/scorecard_report.md
